window.APIONIX_CONFIG = Object.freeze({
  // Replace this URL when the APK Analysis Platform frontend is deployed.
  apkFrontendUrl: "http://127.0.0.1:5173",
  // IoT team's running dashboard; its Vite proxy connects to the IoT backend.
  iotSystemUrl: "http://127.0.0.1:5180",
});
